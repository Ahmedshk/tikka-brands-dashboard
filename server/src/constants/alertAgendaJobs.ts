/** Repeating Agenda job: runs evaluation (interval or fixed-times tick). */
export const ALERTS_EVALUATE_JOB = "alerts:evaluate";

/** One-shot: cancel + recreate repeating alert job from DB settings. */
export const ALERTS_RESCHEDULE_JOB = "alerts:reschedule";
